import { createTheme } from '@mui/material';
const Theme = createTheme({
    palette: {
        primary: {
            main: '#41B6E6',
        },
        secondary: {
            main: '#DB3EB1',
        },
        text: {
            primary: '#000000',
        },
    },
});
export default Theme;
